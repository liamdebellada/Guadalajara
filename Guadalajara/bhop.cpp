#include "bhop.h"
#include "Menu.h"
#include <iostream>
#include "Offsets.h"
#include <Windows.h>
#include "Utils.h"

//int dwForceJumpAdd = 0;
//DWORD client = (DWORD)GetModuleHandle("client.dll");
//DWORD CUNT = 0x524CE94;
//DWORD oldprotect;


void bhop() {
	//if (menu::isBhop) {
	//	Ent* localplayer = (Ent*)Interfaces->ClientEntityList->GetClientEntity(Interfaces->EngineClient->GetLocalPlayer());
	//	if (localplayer != nullptr) {
		//	if (localplayer->m_fFlags == 257) {
				//on ground

				//inline CInput* g_Input = nullptr;
				//g_Input = *(CInput**)(Utils::PatternScan(client, "B9 ? ? ? ? F3 0F 11 04 24 FF 50 10") + 1);
				//auto cmd = g_Input->GetUserCmd(sequence_number);
				//#define IN_JUMP   (1 << 1)
				// cmd->buttons |= IN_JUMP;


				//DWORD ABC = *reinterpret_cast< DWORD*>(client+CUNT);

	//		}
	//	}
//	}
}
